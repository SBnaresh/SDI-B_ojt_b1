#include<stdio.h>
 int main()

 {
      int a[5],i;
      printf("enter array element");
       for(i=0;i<=4;i++)
       {
        scanf("$d",&a[i]);
       }
       printf("reverce array element");
       for(i=4;i>=0;i--)
       {
        scanf("$d",&a[i]);
       }
        printf("%d",a[i]);
            return 0;
 }

 