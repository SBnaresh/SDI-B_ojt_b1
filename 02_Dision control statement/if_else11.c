#include<stdio.h>
int main()
{
  int a,b;
  printf("enter two no ");
  scanf("%d %d",&a,&b);
    if(a>b){
      printf(" a=%d  si grether then b= %d",a,b);
    }
    else{
    printf("a=%d is less then b=%d",a,b);
    }
    
    return 0;

}